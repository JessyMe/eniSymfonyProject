{% extends 'layout.html.twig' %}

{% block title %}Inscription{% endblock %}
{% form_theme registerForm 'bootstrap_4_layout.html.twig' %}
{% block main %}
{#    {% if is_granted("ROLE_USER") %}#}
{#        {% if not user.photo %}#}
{#            {{ asset('photoProfil/profil.jpg') }}#}
{#        {% else %}#}
{#            <div class="container">#}
{#                <div class="photo">#}
{#                    <img src="{{ asset('user.photo') }}" alt="Photo profil">#}
{#                </div>#}
{#            </div>#}
{#        {% endif %}#}
{#    {% endif %}#}

    <div class="container">
        <div class="register">
            <div class="row">
                <div class="col-12">
                    <div class="form-group">
                        <div class="form-label">
                            {{ form_start(registerForm) }}
                            {{ form_widget(registerForm) }}
{#                            {{ form_row(registerForm.photo) }}#}
                            <br>
                            <button class="btn btn-primary" type="submit">Enregistrer</button>
                            <button class="btn btn-dark" type="reset">Annuler</button>
                            {{ form_end(registerForm) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block footer %}{% endblock %}